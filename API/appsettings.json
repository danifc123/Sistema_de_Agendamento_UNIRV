{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Host=${PGHOST:localhost};Port=${PGPORT:5432};Database=${PGDATABASE:sistema_agendamento};Username=${PGUSER:postgres};Password=${PGPASSWORD:fariadocarmo123};SSL Mode=${SSL_MODE:Disable};Trust Server Certificate=${TRUST_CERTIFICATE:true}"
  },
  "JwtSettings": {
    "SecretKey": "${JWT_SECRET_KEY:sua_chave_secreta_muito_longa_e_segura_aqui_minimo_32_caracteres}",
    "Issuer": "${JWT_ISSUER:SistemaAgendamentoUNIRV}",
    "Audience": "${JWT_AUDIENCE:PRAEUsers}",
    "ExpirationHours": "${JWT_EXPIRATION_HOURS:24}"
  },
  "Email": {
    "Provider": "${EMAIL_PROVIDER:SendGrid}",
    "From": "${EMAIL_FROM:daniel.f.carmo@academico.unirv.edu.br}",
    "SendGridApiKey": "${SENDGRID_API_KEY:}"
  }
}
